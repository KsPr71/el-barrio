# Supabase (copiar a .env o .env.local y rellenar)
EXPO_PUBLIC_SUPABASE_URL=https://tu-proyecto.supabase.co
EXPO_PUBLIC_SUPABASE_KEY=tu_anon_key

# Google Maps (react-native-maps) – solo necesario para Android/iOS con mapa embebido
# Crear API key en https://console.cloud.google.com/apis/credentials
# Android: activar "Maps SDK for Android" y restringir la key por SHA-1 + package
# iOS (opcional): activar "Maps SDK for iOS" y restringir por bundle ID
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=
